{
  "name": "flappy-copilot",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "cabinet:setup": "node scripts/setup-cabinet.mjs",
    "test": "npm run test:compat",
    "test:compat": "playwright test",
    "test:negative": "npm run build && node scripts/negative-controls.mjs",
    "test:smoke": "playwright test --config=playwright.smoke.config.js"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
